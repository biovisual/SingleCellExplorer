Require Python 3.5, pip, unzip
sudo apt install python3
sudo apt install python3-pip
 
pip install -r req.txt
gunicorn singleCell.wsgi:application -b 127.0.0.1:8000

mongod -port 27018 -dbpath dbfiles/ --logpth logfiles

### troubleshooting

sudo apt-get install libcurl3


start server:
mongod –port 27018

sudo service mongod start

### start django application
manage.py run server

download mongo gzip files to /scDB
gunzip *.gz
mongorestore -d scDB scDB 
or mongorestore -d scDB --gzip scDB
  
monogod –dbpath scDB –port 27018
